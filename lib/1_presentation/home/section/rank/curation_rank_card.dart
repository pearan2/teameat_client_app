import 'package:flutter/material.dart';

class CurationRankCard extends StatelessWidget {
  const CurationRankCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
